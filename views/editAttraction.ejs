<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Travel App</title>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin-top: 50px;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        h2 {
            margin-bottom: 20px;
            color: #343a40;
        }
        .form-label {
            font-weight: bold;
            color: #495057;
        }
        .form-control {
            margin-top: 5px;
            margin-bottom: 15px;
            padding: 10px;
            border: 1px solid #ced4da;
            border-radius: 4px;
        }
        .img-thumbnail {
            border-radius: 8px;
        }
        .btn-primary {
            background-color: teal;
            border-color: teal;
            transition: transform 0.3s ease, background-color 0.3s ease, border-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: darkcyan;
            border-color: darkcyan;
            transform: scale(1.1);
        }
        .btn-secondary {
            transition: transform 0.3s ease, background-color 0.3s ease, border-color 0.3s ease;
        }
        .btn-secondary:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="text-center"><h2>Update Attraction</h2></div>
        <div class="text-center">
            <% if (attraction) { %>
                <form action="/editAttraction/<%= attraction.attractionId %>" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="countryId" value="<%= attraction.countryId %>">

                    <div class="mb-3">
                        <label for="attractionName" class="form-label">Attraction Name:</label>
                        <input type="text" id="attractionName" name="attractionName" class="form-control" value="<%= attraction.attractionName %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="price" class="form-label">Price:</label>
                        <input type="number" id="price" name="price" class="form-control" value="<%= attraction.price %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="rating" class="form-label">Rating:</label>
                        <input type="number" step="0.1" id="rating" name="rating" class="form-control" value="<%= attraction.rating %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="des" class="form-label">Description:</label>
                        <input type="text" id="des" name="des" class="form-control" value="<%= attraction.description %>" required>
                    </div>

                    <div class="mb-3">
                        <label for="currentImage" class="form-label">Current Image:</label><br>
                        <input type="text" name="currentImage" class="form-control" value="<%= attraction.attractionImage %>" disabled>
                        <img src="/images/<%= attraction.attractionImage %>" class="img-thumbnail mt-2"><br><br>
                    </div>

                    <div class="mb-3">
                        <label for="image" class="form-label">New Image:</label><br>
                        <input type="file" id="image" name="image" class="form-control" accept="image/*"><br><br>
                    </div>

                    <button type="submit" class="btn btn-primary">Update Attraction</button>
                </form>
            <% } else { %>
                <p>No attraction found.</p>
            <% } %>
            <a href="/" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
</body>
</html>
