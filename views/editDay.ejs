<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Student App</title>
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin-top: 50px;
            padding: 20px;
            background-color: #ffffff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        h2 {
            margin-bottom: 20px;
            color: #343a40;
        }
        .form-label {
            font-weight: bold;
            color: #495057;
        }
        .form-control {
            margin-top: 5px;
            margin-bottom: 15px;
            padding: 10px;
            border: 1px solid #ced4da;
            border-radius: 4px;
        }
        .btn-primary {
            background-color: teal;
            border-color: teal;
            transition: transform 0.3s ease, background-color 0.3s ease, border-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: darkcyan;
            border-color: darkcyan;
            transform: scale(1.1);
        }
        .btn-secondary {
            transition: transform 0.3s ease, background-color 0.3s ease, border-color 0.3s ease;
        }
        .btn-secondary:hover {
            transform: scale(1.1);
        }
    </style>
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container">
        <div class="text-center">
            <h2>Update Itinerary</h2>
        </div>
        <div class="text-center">
            <% if (day) { %>
            <form action="/editDay/<%= day.dayID %>" method="POST">
                <input type="hidden" name="itiID" value="<%= day.itiID %>">
                
                <div class="mb-3">
                    <label for="time" class="form-label">Time:</label>
                    <input type="time" id="time" name="time" class="form-control" value="<%= day.time %>" required>
                </div>
                
                <div class="mb-3">
                    <label for="attraction" class="form-label">Event:</label>
                    <input type="text" id="attraction" name="attraction" class="form-control" value="<%= day.attraction %>" required>
                </div>
                
                <div class="mb-3">
                    <label for="day" class="form-label">Day:</label>
                    <input type="number" id="day" name="day" class="form-control" value="<%= day.day %>" required>
                </div>
                
                <div class="mb-3">
                    <label for="cost" class="form-label">Cost:</label>
                    <input type="number" id="cost" name="cost" class="form-control" value="<%= day.cost %>" required>
                </div>
                
                <button type="submit" class="btn btn-primary">Update Day</button>
            </form>
            <% } else { %>
            <p>No Day found.</p>
            <% } %>
            <a href="/" class="btn btn-secondary mt-3">Back</a>
        </div>
    </div>
</body>
</html>
